/** @jsxImportSource @emotion/react */
import { css } from "@emotion/react";

const divStyle = css({display: "flex", height: "30px"});

const inputStyle = css({flex: 8});

const buttonStyle = css({flex: 2});

const chatBoxStyle = css({backgroundColor: "black", color: "white", height: "500px", overflowY: "auto"});

const messageStyle = css({ color: "white" });

const containerStyle = css({ width: "500px", margin: "0 auto" });

export { divStyle, inputStyle, buttonStyle, chatBoxStyle, messageStyle, containerStyle };